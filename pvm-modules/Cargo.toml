[package]
name = "pvm-modules"
version = "0.1.0"
edition = "2024"
description = "PolkaPulse PVM yield optimization and math modules"

[lib]
crate-type = ["cdylib", "rlib"]
path = "src/lib.rs"

[dependencies]

[dev-dependencies]