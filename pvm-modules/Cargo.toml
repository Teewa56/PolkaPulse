[package]
name = "pvm-modules"
version = "0.1.0"
edition = "2024"
description = "PolkaPulse PVM yield optimization and math modules"

[lib]
crate-type = ["cdylib", "rlib"]
path = "src/lib.rs"

[dependencies]
cargo-contract = "5.0.3"
cargo-contracts = "0.0.2"

[dev-dependencies]
